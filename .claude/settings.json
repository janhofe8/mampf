{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.command // \"\"' | grep -q 'nuriruulwjjpycdszdrn\\.supabase\\.co/storage/v1/object' && printf '%s\\n' '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"ask\",\"permissionDecisionReason\":\"Supabase Storage fetch detected — counts toward the 5 GB/month Free egress budget. The April 26 migration session burned 19 GB this way. Confirm only if necessary; prefer ~/Food Bilder/ mirror for image content.\"}}' || true"
          }
        ]
      }
    ]
  }
}
